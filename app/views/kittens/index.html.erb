<h1>All Kittens</h1>
<ul>
  <% @kittens.each do |kitten| %>
    <li>
      <%= kitten.name %>
      <%= link_to 'Delete', kitten, method: :delete, data: { confirm: 'Are you sure you want to delete this kitten?' } %>
    </li>
  <% end %>
</ul>
